<div class="navbar navbar-inverse navbar-fixed-top" role="navigation">
      <div class="container-fluid">
        <div class="navbar-header">
          <button type="button" class="navbar-toggle collapsted" data-toggle="collapse" data-target="#navbar" aria-expanded="false" aria-controls="navbar">
            <span class="sr-only">Toggle navigation</span>
            <span class="icon-bar"></span>
            <span class="icon-bar"></span>
            <span class="icon-bar"></span>
          </button>
        
        </div>
        <div id="navbar" class="navbar-collapse collapse">
          <ul class="nav navbar-nav navbar-left">
            <% if current_user %>
              <p>Signed in as <%= current_user.email %></p>
              <li><%= link_to "Sign out", destroy_user_session_path, method: :delete %></li>
            <% end %>

            <li class="list-inline-item"> 
              <%= link_to "Store Home", store_path %> </li>

            <% if !current_user %>
              <li><%= link_to "sign in", new_user_session_path %></li>
              <li><%= link_to "Sign up", new_user_registration_path %></li>
            <% end %>

            <% if current_user && current_user.current_cart %>
              <li><%= link_to "Cart", cart_path(current_user.current_cart) %></li>
            <% end %>
          </ul>
        </div>
      </div>
    </div>